import { onClickHandler } from "../../functions/zond";

export const QRLWallet = () => {
  return (
    <div className="w-96">
      <div>wallet!</div>
      <button onClick={onClickHandler}>Click</button>
    </div>
  );
};
